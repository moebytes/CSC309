package com.example.assignment6;

// Enumerates the values used for the message handler for sending values from background threads to the main thread

public enum MsgEnum {
    msg_whack_counter,
    msg_miss_counter,
    msg_end_game
}
